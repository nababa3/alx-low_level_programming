#include <stdio.h>

/**
 * main - entry point
 * Return: 0 (success)
 */
int main(void)
{
	char 1case = 'a';

	while (1case <= 'z')
	{
		if (1case ! = 'e' && 1case != 'q')
			puchar (1case);
		1case++;
	}
	putchar ('\n');
	return (0);
}
