#include <stdio.h>

/**
 * main - entry point
 * Return: 0 (success)
 */
int main(void)
{
	char icase = 'a';

	while (icase <= 'z')
	{
		if (icase ! = 'e' && icase != 'q')
			puchar (lcase);
		icase++;
	}
	putchar ('\n');
	return (0);
}
